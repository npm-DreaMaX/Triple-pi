{
  "name": "triple-pi",
  "version": "0.3.0",
  "description": "Personal Coding Agent — Pi Runtime + OpenClaw-style async memory extraction",
  "type": "module",
  "private": true,
  "scripts": {
    "install-ext": "mkdir -p ~/.pi/agent/extensions && ln -sf $(pwd)/extensions/memory ~/.pi/agent/extensions/memory",
    "extract": "node scripts/extract.mjs",
    "cron:install": "node scripts/install-cron.mjs",
    "cron:remove": "node scripts/remove-cron.mjs",
    "setup": "cd pi-runtime && npm install --ignore-scripts && npm run build && cd .. && npm run install-ext && npm run cron:install"
  }
}
